import { Counter } from "widgets/Counter";
import { MainImage } from "widgets/MainImage";

const MainPage = () => {
    return (
        <div>
            <MainImage />
            <Counter />
        </div>
    );
}

export default MainPage